#include <iostream>
#include "test.h"

int main()
{
	/*
	someNums* a;
	memset(a, 0, sizeof(someNums));
	a->size = 0xff;

	FILE* fp = fopen("test.output", "wb");
	write_someNums(fp, &a);
	fclose(fp);*/

	return 0;
}
